import { Provider } from 'react-wrap-balancer'

import OnePager from './OnePager'

function App() {
    return (
        <Provider>
            <OnePager />
        </Provider>
    )
}

export default App
